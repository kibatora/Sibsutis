# 1. Используем официальный образ Python как основу
FROM python:3.9-slim

# 2. Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# 3. Копируем файл с зависимостями
COPY requirements.txt .

# 4. Устанавливаем зависимости
RUN pip install --no-cache-dir -r requirements.txt

# 5. Копируем весь остальной код в рабочую директорию
COPY . .

# 6. Указываем команду, которая будет выполняться при запуске контейнера
# Для бэкенда это будет запуск Flask-сервера, для симулятора - запуск скрипта.
# Мы переопределим эту команду в docker-compose.yml, но здесь она для примера.
CMD ["python3", "app.py"]